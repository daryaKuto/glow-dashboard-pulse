/**
 * Shared data types
 * 
 * Common types used across the data layer.
 * Feature-specific types should live in their respective feature modules.
 */

// Re-export commonly used types from integrations
export type { Database } from '@/integrations/supabase/types';

